name_m1, hp_1, dmg_1 = input("введите параметры монстра(имя здоровье урон)").split()
name_m2, hp_2, dmg_2 = input("введите параметры монстра(имя здоровье урон)").split()
m1 = Monster(name=name_m1, hp=hp_1, dmg=dmg_1)
m2 = Monster(name=name_m2, hp=hp_2, dmg=dmg_2)

print()
print("В бою участвуют:")
print(m1.name, m1.hp, m1.dmg)
print(m2.name, m2.hp, m2.dmg)
print()
print("------Бой начался-----")
print()

while True:
    print(f"{m1.name} наносит удар равный {m1.dmg}")
    m2.hp -= m1.dmg
    if m2.hp <= 0:
        print(f"У {m2.name} осталось 0 здоровья")
        print(f"{m1.name} выиграл!!!")
        winner = m1.name
        break
    else:
        print(f"У {m2.name} осталось {m2.hp} здоровья")
        print(f"{m2.name} наносит удар равный {m2.dmg}")
        m1.hp -= m2.dmg
        if m1.hp <= 0:
            print(f"У {m1.name} осталось 0 здоровья")
            print(f"{m2.name} выиграл!!!")
            winner = m2.name
            break
        else:
            print(f"У {m1.name} осталось {m1.hp} здоровья")
            print()
            print()

print()
print(f"Игра окончена победил: {winner}")
